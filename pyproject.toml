[project]
name = "progress-tracker"
version = "0.1.0"
description = "Multi-machine progress dashboard for embodied_perceptron's automation/ experiment runs."
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
    "pandas>=2.0",
]

[tool.uv]
package = false
